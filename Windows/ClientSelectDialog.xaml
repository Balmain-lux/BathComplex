<Window x:Class="BathComplex.Windows.ClientSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BathComplex.Windows"
        mc:Ignorable="d"
        Title="Выбор клиента"
        Height="500" Width="650"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A1A"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary Source="../Resources/Styles.xaml"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Выберите клиента" Style="{StaticResource SubTitleLabel}" Margin="0,0,0,10"/>

        <!-- Поиск -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="txtSearch" Style="{StaticResource DarkTextBox}" Width="300"
                     TextChanged="txtSearch_TextChanged"/>
            <Button x:Name="btnSearch" Content="🔍 Найти"
                    Style="{StaticResource GoldButton}" Margin="10,0,0,0" Click="btnSearch_Click"/>
        </StackPanel>

        <!-- Список клиентов -->
        <DataGrid x:Name="dgClients" Grid.Row="2"
                  Style="{StaticResource DarkDataGrid}"
                  ColumnHeaderStyle="{StaticResource DarkDataGridHeader}"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  MouseDoubleClick="dgClients_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ClientID}" Width="50"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="250"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="150"/>
                <DataGridTextColumn Header="Скидка" Binding="{Binding DiscountPercent, StringFormat={}{0}%}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnCancel" Content="Отмена"
                    Style="{StaticResource OutlineButton}" Click="btnCancel_Click"/>
            <Button x:Name="btnSelect" Content="Выбрать"
                    Style="{StaticResource GoldButton}" Margin="15,0,0,0" Click="btnSelect_Click"/>
        </StackPanel>
    </Grid>
</Window>
