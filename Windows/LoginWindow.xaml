<Window x:Class="BathComplex.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BathComplex.Windows"
        mc:Ignorable="d"
       Title="Вход в систему - Банные Апартаменты"
        Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#F5F3EE"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <ResourceDictionary Source="../Resources/Styles.xaml"/>
    </Window.Resources>


    <Border Background="#F5F3EE" CornerRadius="10" BorderBrush="#EDE8E0" BorderThickness="1" >
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Кнопка закрытия -->
            <Button x:Name="btnClose" Content="✕" Style="{StaticResource OutlineButton}"
                    HorizontalAlignment="Right" Width="35" Height="35" Padding="0"
                    Foreground="#7A756D" BorderThickness="0" FontSize="16"
                    Click="btnClose_Click"/>

            <!-- Логотип -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,25">
                <Label Content="БАННЫЕ" Foreground="#B8943A" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" FontFamily="{StaticResource MainFont}"/>
                <Label Content="АПАРТАМЕНТЫ" Foreground="#2D2A26" FontSize="16"
                       HorizontalAlignment="Center" FontFamily="{StaticResource MainFont}"/>
                <Label Content="Система управления" Foreground="#7A756D" FontSize="12"
                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Форма входа -->
            <StackPanel Grid.Row="2">
                <Label Content="Логин" Foreground="#7A756D" FontSize="13" Margin="0,0,0,5"/>
                <TextBox x:Name="txtLogin" Style="{StaticResource DarkTextBox}"
                         Height="60" FontSize="15"
                         KeyDown="TextBox_KeyDown"/>

                <Label Content="Пароль" Foreground="#7A756D" FontSize="13" Margin="0,15,0,5"/>
                <PasswordBox x:Name="txtPassword" 
                             Background="#EDE8E0" Foreground="#2D2A26"
                             BorderBrush="#7A756D" BorderThickness="1"
                             FontSize="15" Height="60" Padding="10,8"
                             CaretBrush="#B8943A"
                             KeyDown="TextBox_KeyDown"/>

                <Label x:Name="lblError" Foreground="#B85450" FontSize="12"
                       HorizontalAlignment="Center" Margin="0,10,0,0" Visibility="Collapsed"/>
            </StackPanel>

            <!-- Кнопка входа -->
            <Button x:Name="btnLogin" Grid.Row="4" Content="ВОЙТИ"
                    Style="{StaticResource GoldButton}"
                    Height="45" FontSize="16" FontWeight="Bold"
                    Margin="0,15,0,0"
                    Click="btnLogin_Click"/>

            <!-- Тестовые данные -->
            
        </Grid>
    </Border>
</Window>
