<Window x:Class="BathComplex.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BathComplex.Windows"
        mc:Ignorable="d"
       Title="Вход в систему - Банные Апартаменты"
        Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A1A"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <ResourceDictionary Source="../Resources/Styles.xaml"/>
    </Window.Resources>


    <Border Background="#1A1A1A" CornerRadius="10" BorderBrush="#3C3C3C" BorderThickness="1" >
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Кнопка закрытия -->
            <Button x:Name="btnClose" Content="✕" Style="{StaticResource OutlineButton}"
                    HorizontalAlignment="Right" Width="35" Height="35" Padding="0"
                    Foreground="#A09888" BorderThickness="0" FontSize="16"
                    Click="btnClose_Click"/>

            <!-- Логотип -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,25">
                <Label Content="БАННЫЕ" Foreground="#C5A46B" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" FontFamily="{StaticResource MainFont}"/>
                <Label Content="АПАРТАМЕНТЫ" Foreground="#F5F0E8" FontSize="16"
                       HorizontalAlignment="Center" FontFamily="{StaticResource MainFont}"/>
                <Label Content="Система управления" Foreground="#A09888" FontSize="12"
                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Форма входа -->
            <StackPanel Grid.Row="2">
                <Label Content="Логин" Foreground="#A09888" FontSize="13" Margin="0,0,0,5"/>
                <TextBox x:Name="txtLogin" Style="{StaticResource DarkTextBox}"
                         Height="60" FontSize="15"
                         KeyDown="TextBox_KeyDown"/>

                <Label Content="Пароль" Foreground="#A09888" FontSize="13" Margin="0,15,0,5"/>
                <PasswordBox x:Name="txtPassword" 
                             Background="#3C3C3C" Foreground="#F5F0E8"
                             BorderBrush="#555555" BorderThickness="1"
                             FontSize="15" Height="60" Padding="10,8"
                             CaretBrush="#C5A46B"
                             KeyDown="TextBox_KeyDown"/>

                <Label x:Name="lblError" Foreground="#B85450" FontSize="12"
                       HorizontalAlignment="Center" Margin="0,10,0,0" Visibility="Collapsed"/>
            </StackPanel>

            <!-- Кнопка входа -->
            <Button x:Name="btnLogin" Grid.Row="4" Content="ВОЙТИ"
                    Style="{StaticResource GoldButton}"
                    Height="45" FontSize="16" FontWeight="Bold"
                    Margin="0,15,0,0"
                    Click="btnLogin_Click"/>

            <!-- Тестовые данные -->
            
        </Grid>
    </Border>
</Window>
